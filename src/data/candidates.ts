/** Equivalente a /data/candidates.js — fonte única de candidatos da cola. */
export {
  COLA_CANDIDATES as candidates,
  COLA_DEMO_DISCLAIMER,
  EMPTY_SELECTION,
  POSITIONS,
} from "./cola-candidates";
export type { Candidate, PositionKey } from "./cola-candidates";
